# S4 全タスクリスト統合仕様

## 入力と正本

`eスポーツ大会_開催設計_全タスクリスト.md`（SHA-256 `D24C5785D0AA8D3D4995767EAB565016E346149294ABEB0E0133C163C0E2BE87`、300行）をS4としてsource catalogへ追加した。旧S1〜S3は保持する。P0-01〜P6-07の各タスクは、Markdown表の1行を1件として正確な行番号を持つ。

## データ判断

- 表の状態値を初期値とするため、未着手58、進行中15、完了0、保留0。
- 担当は表セルの文字列と分割配列を無損失で保持し、主要8名の検索キーだけを別導出する。「全員」は8名全員へ展開し、責任者は担当者フィルタへ混ぜない。
- `大会運営` は既存正式名 `大会運営チーム（Tournament Admin）` へ正規化する。未知の原文チーム名は管理部へ丸めず拒否する。
- スン関与4件はP0-07依存のブロックと解除条件を別フィールドで表し、原表の初期状態を変えない。
- P0-01は旧期限「7月中」の超過注記と改訂期限2026-08-18を別表示する。
- `M/D`だけが確定できる期限にISO日付を付ける。期間、月旬、継続、開催前後などは原文だけを保持し、超過判定しない。P3の1月期限は2027年。
- Phase 1/2の期間重複は、現在Phase表示では2026年9月をPhase 1、10月以降をPhase 2として優先する。
- 開催日は2027年3月内で未確定。ダッシュボードは「3月開始まで」を数え、大会当日までとは表示しない。

## 移行

schema v2読込時は旧タスク配列を `migrationArchive` へそのまま退避し、P73をアクティブ化する。flow、viewport、auditは継承する。schema v3再読込は同じarchiveを再追加しない。現行schema v4への移行でもarchiveを保持し、export bundleに含める。

正本73件は削除不可とし、状態変更と編集のみ許可する。追加したcustom taskは削除でき、依存・canvas参照を同時に除去して操作auditへ記録する。

importでは出典1件以上、正本IDごとのS4期待行/hash、rawTeam対応、finite座標、node task参照、edge ID一意性、audit全項目・分類・日時・配列・ID一意性、KPI finite/非負を検証する。いずれか1件でも不正なら保存状態を変更しない。

隔週報告は比較基準の状態から現在状態への遷移だけを抽出し、block/保留注記を同じ行に載せる。変更タスクを「次の2週間」へ再掲せず、改行と見出し文字を除去する。

## 受入オラクル

- Phase: `9/10/18/10/11/8/7`
- 緊急度: 高34 / 中34 / 低5
- 状態: 未着手58 / 進行中15 / 完了0 / 保留0
- 責任者: 鈴木20 / ウメノ18 / ロブ14 / ウニュ8 / ユウタ8 / 浜名5
- 13チーム: `6/6/5/5/8/8/8/5/6/6/1/1/8`

unit、UI、a11y、E2Eで上記と主要操作を検証する。
